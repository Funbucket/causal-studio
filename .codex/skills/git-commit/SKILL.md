---
name: git-commit
description: Analyze repository changes and create commits with a minimal direct flow. Use when asked to check status/diff, suggest a commit message, stage files, or run git commit.
---

# Git Commit

Use direct git commands first.

## Quick Run

```bash
git status --short
git diff --stat
git add -A
git commit -m "docs: update content"
```

## Optional Checks

```bash
git diff --staged
git log --oneline -5
```
